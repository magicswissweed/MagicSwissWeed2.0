###
# @name Get Samples of last 40 days for multiple stations
# make sure the stationIds do not not have a forecast (-> this probably means, there is a last40Days available)
#
POST {{base-path}}/sample/last40Days
Content-Type: application/json
Authorization: Bearer {{auth_token}}

[
  {
    "country": "CH",
    "externalId": "2243"
  }
]
